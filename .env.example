# Public configuration only. Never place passwords, JWT secrets, or database URLs here.
# For local development, point this at the approved non-production mobile API.
EXPO_PUBLIC_API_BASE_URL=https://your-preview-host.example/api/mobile/v1
